name: CD FastApi1 Workflow
on:
  workflow_dispatch:

jobs:
  my_build:
    name: fastAPI-app
    runs-on: ubuntu-latest

    steps:
      - name: Checking out our code
        uses: actions/checkout@v6